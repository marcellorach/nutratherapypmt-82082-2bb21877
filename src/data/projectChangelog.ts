// Shim re-export — a fonte de verdade é CHANGELOG.md, regenerado pelo
// script `npm run sync:changelog` em src/data/projectChangelog.generated.ts.
// NÃO adicione entradas aqui à mão.
export { changelog, lastChangelogDate } from "./projectChangelog.generated";
export type {
  ChangelogEntry,
  ChangelogKind,
  ChangelogStatus,
} from "./projectChangelog.generated";
